const FindPeopleResultList = (users) => {
    console.log(users);
    return (
        <ul>
            {/* {users.map((user) => (
                <li key={user.name}>{user.name}</li>
            ))} */}
        </ul>
    );
};

export default FindPeopleResultList;
